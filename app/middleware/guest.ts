export default defineNuxtRouteMiddleware((_to, _from) => {
  // Guest-only middleware placeholder. If needed, redirect authenticated users away.
  // const auth = useAuth()
  // if (auth.status.value === 'authenticated') return navigateTo('/')
})