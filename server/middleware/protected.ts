// No-op middleware placeholder. Required to export a default handler
// so Nitro can import it without Rollup errors.
export default defineEventHandler((_event) => {
	// Intentionally left blank
})

