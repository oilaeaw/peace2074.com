# Netlify Configuration for peace2074.com
# This is a Vue 3 SPA with a separate Nitro API backend
# No prerendering or SSR required - pure client-side rendering

[build]
  command = "pnpm install -r && pnpm build && pnpm --filter nitro-api build"
  publish = "dist"
  functions = "apps/nitro-api/.netlify/functions-internal"

# Note: Legacy prerendering must be disabled in Netlify dashboard
# See DISABLE_LEGACY_PRERENDERING.md for instructions

# API routes should be handled by Nitro functions
# Routes /api/auth/login -> /.netlify/functions-internal/server/auth/login
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions-internal/server/:splat"
  status = 200

# SPA fallback for all other routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
